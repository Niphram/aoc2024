{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "by-gdb",
      "request": "launch",
      "name": "Debug current package",
      "program": "bin/${relativeFileDirname}",
      "cwd": "${workspaceRoot}",
      "preLaunchTask": "compile-debug"
    }
  ]
}
